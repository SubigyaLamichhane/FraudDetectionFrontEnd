import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'frauddetection.subigya.tech',
  appName: 'Fraud Detection',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
